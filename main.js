const Shape = require('./Develop/lib/shapes');
const Text = require('./Develop/lib/text');  

const shapeInstance = new Shape(); 

shapeInstance.run(); 

const textInstance = new Text(); 

    textInstance.run();